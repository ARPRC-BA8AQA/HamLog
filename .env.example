# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# Prefer the hex form for deployments. It must contain exactly 64 hex characters.
HAMLOG_AES_KEY=

# Alternative: URL-safe base64 encoding of exactly 32 bytes.
HAMLOG_AES_KEY_B64=
